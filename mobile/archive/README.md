# Archived Mobile Code

This folder contains legacy/archived mobile application files that are not part of the active app.

- `App.tsx` and the old `src/navigation` / `src/screens` layout have been moved here for reference.
- The active Expo app is now organized under `mobile/app` and `mobile/src`.

Do not modify archived code unless you are restoring or comparing old mobile behavior.
